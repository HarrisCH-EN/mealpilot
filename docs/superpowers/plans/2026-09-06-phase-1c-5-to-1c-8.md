# Phase 1C-5 to 1C-8 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete Recommendation Apply revalidation, family-level restriction enforcement, stable API error semantics, and safely gated real-MySQL integration verification.

**Architecture:** Keep the existing Express routes, service functions, and MySQL transactions. Add only local validation helpers, transaction-safe preflight checks, and focused tests; do not introduce ORM, repositories, queues, or schema changes.

**Tech Stack:** Node.js, Express, mysql2/promise, Node test runner, MySQL.

**Spec:** User-provided Phase 1C-5 → 1C-8 request in the current task.

## Global Constraints

- Execute phases strictly in order: 1C-5 → 1C-6 → 1C-7 → 1C-8.
- Stop on a real blocker; never use the `mealpilot` business database for destructive or test writes.
- Preserve 1C-1 through 1C-4, existing frontend behavior, and all unrelated worktree changes.
- Do not modify Schema, SQL schema files, frontend, or deferred features.

---

### Task 1: 1C-5 Recommendation Apply Revalidation

**Files:**
- Modify: `server/src/services/recommendation-run-service.js`
- Modify: `server/src/services/menu-item-service.js` only if shared idempotent behavior requires it
- Test: `server/test/recommendation-apply-revalidation.test.js`

- [x] Write failing tests for invalid creator, cross-family recipe, deleted/missing recipe, rollback, idempotency, and source/note preservation.
- [x] Run the focused tests and confirm expected failures.
- [x] Add all preflight checks before Menu/MenuItem writes on the same transaction connection.
- [x] Convert stale Recipe failures to 409 without leaking SQL details.
- [x] Run focused tests and backend full tests.

### Task 2: 1C-6 Restriction Enforcement

**Files:**
- Modify: `server/src/routes/menus.js` or `server/src/services/recommendation-service.js` only where current recommendation candidate filtering is implemented
- Test: `server/test/restriction-enforcement.test.js`

- [x] Write tests for active-member union, left-member exclusion, empty restrictions, and cross-family isolation.
- [x] Run the focused tests and verify the existing family-scoped query.
- [x] Ensure candidate recipes and restrictions are both family-scoped.
- [x] Run focused tests and backend full tests.

### Task 3: 1C-7 API Error Contract Cleanup

**Files:**
- Modify: `server/src/http.js`
- Modify: `server/src/routes/recipes.js`
- Modify: `server/src/routes/menus.js`
- Modify: `server/src/routes/auth-family.js`
- Modify: related middleware only when required by the contract
- Test: `server/test/api-error-contract.test.js`

- [x] Write failing tests for 400/401/403/404/409/422 and database-error redaction.
- [x] Run the focused tests and confirm expected failures.
- [x] Add minimal request validation and stable error mapping without changing success response shapes.
- [x] Run focused tests and backend full tests.

### Task 4: 1C-8 Safe Integration Verification

**Files:**
- Create: `server/test/integration/phase-1c.integration.test.js`
- Modify: `server/package.json` only if an explicit safe integration command is needed

- [x] Detect an explicitly configured isolated test database, such as `MYSQL_TEST_DATABASE`.
- [x] If absent, skip all write tests and report the missing safe database; never fall back to `mealpilot`.
- [x] Use isolated prefixed data and cleanup/rollback for schema, transaction, concurrency, isolation, restriction, lifecycle, and dynamic-reference cases.
- [x] Run integration tests only when the safety gate proves the database is isolated.
- [x] Run backend tests, syntax checks, diff checks, and configured lint.
