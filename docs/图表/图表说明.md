# 图表说明

更新时间：2026-09-18（Asia/Shanghai）

## 文件与状态

| 图表 | 可编辑源 | PNG | 当前内容 |
| --- | --- | --- | --- |
| 系统总体架构图 | [drawio](系统总体架构图.drawio) | [PNG](系统总体架构图.png) | 小程序、CloudBase 云托管、Express、MySQL、私有 Storage、微信登录 |
| 系统总体 E-R 图 | [drawio](系统总体E-R图.drawio) | [PNG](系统总体E-R图.png) | 当前 19 张表、主外键和核心唯一约束 |
| 系统功能模块图 | [drawio](系统功能模块图.drawio) | [PNG](系统功能模块图.png) | 登录、家庭、菜谱、菜单、推荐、偏好、忌口、标签、反馈与洞察 |
| 菜谱推荐处理流程图 | [drawio](菜谱推荐处理流程图.drawio) | [PNG](菜谱推荐处理流程图.png) | 校验、硬过滤、结构组合、评分、候选持久化和事务应用 |

## 同步规则

- draw.io 文件是唯一可编辑图表源；打开后如果修改节点，需同步 MermaidData（若文件中存在）和本文的说明。
- PNG 是现有导出版本。本机没有可用的 draw.io CLI，本次已更新架构 drawio 源文件，但没有伪造 PNG 已重新导出的结论；需要在 draw.io 中手动打开四个源文件并重新导出 PNG。
- 图表中的生产事实以代码和正式文档为准，不能从 PNG 反推接口或数据库约束。

## 核对重点

1. 架构图必须显示 `mealpilot-api` CloudBase 云托管、MySQL `mealpilot`、CloudBase 私有 Storage 和微信 `code2Session`。
2. E-R 图必须覆盖 `database/01_schema.sql` 的 19 张表，并保留 `recommendation_items` 兼容表。
3. 推荐流程图必须体现候选最多 3 个、`candidateId` 应用前复核、家庭/成员边界和事务。
4. 模块图中的页面、后端路由和数据域要与当前代码命名一致。
